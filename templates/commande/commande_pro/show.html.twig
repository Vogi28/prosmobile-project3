{% extends 'base.html.twig' %}

{% block title %}CommandePro{% endblock %}

{% block body %}
    <h1>CommandePro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_pro.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_pro_index') }}">back to list</a>

    <a href="{{ path('commande_pro_edit', {'id': commande_pro.id}) }}">edit</a>

    {{ include('commande_pro/_delete_form.html.twig') }}
{% endblock %}
