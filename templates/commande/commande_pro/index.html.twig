{% extends 'base.html.twig' %}

{% block title %}CommandePro index{% endblock %}

{% block body %}
    <h1>CommandePro index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande_pro in commande_pros %}
            <tr>
                <td>{{ commande_pro.id }}</td>
                <td>
                    <a href="{{ path('commande_pro_show', {'id': commande_pro.id}) }}">show</a>
                    <a href="{{ path('commande_pro_edit', {'id': commande_pro.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commande_pro_new') }}">Create new</a>
{% endblock %}
