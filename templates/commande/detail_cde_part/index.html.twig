{% extends 'base.html.twig' %}

{% block title %}DetailCdePart index{% endblock %}

{% block body %}
    <h1>DetailCdePart index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomArt</th>
                <th>Quantite</th>
                <th>PrixHt</th>
                <th>PrixTtc</th>
                <th>Promo</th>
                <th>Total</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for detail_cde_part in detail_cde_parts %}
            <tr>
                <td>{{ detail_cde_part.id }}</td>
                <td>{{ detail_cde_part.nomArt }}</td>
                <td>{{ detail_cde_part.quantite }}</td>
                <td>{{ detail_cde_part.prixHt }}</td>
                <td>{{ detail_cde_part.prixTtc }}</td>
                <td>{{ detail_cde_part.promo }}</td>
                <td>{{ detail_cde_part.total }}</td>
                <td>
                    <a href="{{ path('detail_cde_part_show', {'id': detail_cde_part.id}) }}">show</a>
                    <a href="{{ path('detail_cde_part_edit', {'id': detail_cde_part.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('detail_cde_part_new') }}">Create new</a>
{% endblock %}
