{% extends 'base.html.twig' %}

{% block title %}PROSMOBILE{% endblock %}

{% block body %}
    <h1>Commandes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ClientID</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande_par in commande_pars %}
            <tr>
                <td>{{ commande_par.id }}</td>
                <td>{{ commande_par.particulier.id }}</td>
                <td>
                    <a href="{{ path('commande_show', {'id': commande_par.id}) }}">details</a>
                    <a href="{{ path('commande_edit', {'id': commande_par.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commande_new') }}">Create new</a>
{% endblock %}
