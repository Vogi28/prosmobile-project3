{% extends 'base.html.twig' %}

{% block title %}PROSMOBILE{% endblock %}

{% block body %}
	{# tree #}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb bg-white rounded-0">
			<li class="breadcrumb-item">
				<a href="{{ path('home') }}">
					Accueil
				</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{ path('particulier_profile', {'id' : app.user.id}) }}">
					Mon profil
				</a>
			</li>
			<li class="breadcrumb-item active" aria-current="page">
				Mes commandes
			</li>
		</ol>
	</nav>
	{# title #}
	<h1 class="text-center">Mes commandes</h1>
	{# body #}
	<table class="table mt-4">
		<thead class="thead-dark">
			<tr>
				<th>Id</th>
				<th>ClientID</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for commande_par in commande_pars %}
				<tr>
					<td>{{ commande_par.id }}</td>
					<td>{{ commande_par.particulier.id }}</td>
					<td>
						<a href="{{ path('commande_show', {'id': commande_par.id}) }}">details</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="2">Aucune commande trouvée</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
