{% extends 'base.html.twig' %}

{% block title %}CommandePar{% endblock %}

{% block body %}
    <h1>CommandePar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_par.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_par_index') }}">back to list</a>

    <a href="{{ path('commande_par_edit', {'id': commande_par.id}) }}">edit</a>

    {{ include('commande_par/_delete_form.html.twig') }}
{% endblock %}
