{% extends 'base.html.twig' %}

{% block title %}PROSMobile - Configuration de la marque{% endblock %}

{% block body %}
	{# tree #}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb bg-white rounded-0">
			<li class="breadcrumb-item">
				<a href="{{ path('admin_index') }}">
					Dashboard
				</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{ path('marque_index') }}">
					Gestion des marques
				</a>
			</li>
			<li class="breadcrumb-item active" aria-current="page">
				Configurer la marque
			</li>
		</ol>
	</nav>
	{# title #}
	<h1>Configuration de la marque</h1>
	{# include brand form #}
	<div class="d-flex justify-content-center">
		{{ include('marque/_form.html.twig', {'button_label': 'Update'}) }}
	</div>
	{# include delete button #}
	<div class="d-flex justify-content-center mb-3">
		{{ include('marque/_delete_form.html.twig') }}
	</div>
{% endblock %}
