{% extends 'base.html.twig' %}

{% block title %}Edit ArtComp{% endblock %}

{% block body %}
	{# tree #}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb bg-white rounded-0">
			<li class="breadcrumb-item">
				<a href="{{ path('admin_index') }}">
					Dashboard
				</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{ path('art_comp_index') }}">
					Gestion des articles compatibles
				</a>
			</li>
			<li class="breadcrumb-item active" aria-current="page">
				Configurer le produit compatible
			</li>
		</ol>
	</nav>
	{# body #}
	<h1>Caractéristique du produit compatible</h1>
	{# include art_comp form #}
	{{ include('art_comp/_form.html.twig', {'button_label': 'Update'}) }}
	{# delete button #}
	<div class="container mt-2">
		<div class="row">
			{{ include('art_comp/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
