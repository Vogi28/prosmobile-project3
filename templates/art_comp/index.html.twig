{% extends 'base.html.twig' %}

{% block title %}ArtComp index{% endblock %}

{% block body %}
    <h1>ArtComp index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ArtId</th>
                <th>ArtCompId</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for art_comp in art_comps %}
            <tr>
                <td>{{ art_comp.id }}</td>
                <td>{{ art_comp.artId }}</td>
                <td>{{ art_comp.artCompId }}</td>
                <td>
                    <a href="{{ path('art_comp_show', {'id': art_comp.id}) }}">show</a>
                    <a href="{{ path('art_comp_edit', {'id': art_comp.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('art_comp_new') }}">Create new</a>
{% endblock %}
