{% extends 'base.html.twig' %}

{% block title %}DetailCdePro{% endblock %}

{% block body %}
    <h1>DetailCdePro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_cde_pro.id }}</td>
            </tr>
            <tr>
                <th>NomArt</th>
                <td>{{ detail_cde_pro.nomArt }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ detail_cde_pro.quantite }}</td>
            </tr>
            <tr>
                <th>PrixHt</th>
                <td>{{ detail_cde_pro.prixHt }}</td>
            </tr>
            <tr>
                <th>Remise</th>
                <td>{{ detail_cde_pro.remise }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ detail_cde_pro.total }}</td>
            </tr>
        </tbody>
    </table>
    {% if is_granted('ROLE_PARTICULIER') %}
        <a href="{{ path('detail_cde_pro_index') }}">back to list</a>
        <a href="{{ path('detail_cde_pro_edit', {'id': detail_cde_pro.id}) }}">edit</a>
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('admin_commande_detail') }}">back to list</a>
        <a href="{{ path('admin_commande_edit', {'id': detail_cde_pro.id}) }}">edit</a>
    {% endif %}

    {{ include('detail_cde_pro/_delete_form.html.twig') }}
{% endblock %}
