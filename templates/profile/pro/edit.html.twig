{% extends 'base.html.twig' %}

{% block title %}PROSMOBILE{% endblock %}

{% block body %}
	{# tree #}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb bg-white rounded-0">
			{# administrator #}
			{% if is_granted('ROLE_ADMIN') %}
				<li class="breadcrumb-item">
					<a href="{{ path('admin_index') }}">
						Dashboard
					</a>
				</li>
				<li class="breadcrumb-item">
					<a href="{{ path('pro_index') }}">
						Comptes professionnels
					</a>
				</li>
			{% else %}
				<li class="breadcrumb-item">
					<a href="{{ path('home') }}">
						Accueil
					</a>
				</li>
				<li class="breadcrumb-item">
					<a href="{{ path('pro_profile', { 'id' : app.user.id }) }}">
						Mon profil
					</a>
				</li>
			{% endif %}
			<li class="breadcrumb-item active" aria-current="page">
				Modifier le profil
			</li>
		</ol>
	</nav>
	{# title #}
	<h1>Modification des informations</h1>
	{# include professional form #}
	<div class="d-flex justify-content-center">
		{{ include('profile/pro/_form.html.twig', {'button_label': 'Update'}) }}
	</div>
	{# administrator #}
	{% if is_granted('ROLE_ADMIN') %}
		{# include delete button #}
		<div class="d-flex justify-content-center mb-3">
			{{ include('profile/pro/_delete_form.html.twig') }}
		</div>
	{% endif %}
{% endblock %}
