{% extends 'base.html.twig' %}

{% block title %}PROSMOBILE{% endblock %}

{% block body %}
<!-- tree -->
{% if is_granted('ROLE_ADMIN') %}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb bg-white rounded-0">
			<li class="breadcrumb-item">
				<a href="{{ path('admin_index') }}">
					Dashboard
				</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{ path('pro_index') }}">
					Index pro
				</a>
			</li>
			<li class="breadcrumb-item">
				<a href="{{ path('pro_show', {'id': pro.id}) }}">
				Profile
                </a>
			</li>
{% elseif is_granted('ROLE_PRO') %}
    <nav aria-label="breadcrumb">
		<ol class="breadcrumb bg-white rounded-0">
			<li class="breadcrumb-item">
				<a href="{{ path('pro_profile', {'id' : app.user.id}) }}">
					Profile
				</a>
			</li>
{% endif %}
            <li class="breadcrumb-item active" aria-current="page">
				Edit
			</li>
		</ol>
	</nav>

    <h1 class="text-center">Editer informations</h1>
<div class="d-flex justify-content-center">
    <p>{{ include('profile/pro/_form.html.twig', {'button_label': 'Update'}) }}</p>
</div>
    {% if is_granted('ROLE_ADMIN') %}
<div class="d-flex justify-content-center">
        <p>{{ include('profile/pro/_delete_form.html.twig') }}</p>
</div>
    {% endif %}
{% endblock %}
