{% extends 'base.html.twig' %}

{% block title %}PROSmobile |catégories{% endblock %}

{% block body %}
    {# tree #}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-white rounded-0">
            <li class="breadcrumb-item">
                <a href="{{ path('admin_index') }}">
                    Dashboard
                </a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">
                Gestion des catégories d'articles
            </li>
        </ol>
    </nav>
    {# body #}
    <h1 class="text-center">Liste des catégories d'articles</h1>
    {# add button #}
    <div class="container-fluid mt-4">
        <div class="row">
            <a class="btn" href="{{ path('type_art_new') }}" title="Ajouter">
                <i data-fa-symbol="add" class="fas fa-plus text-secondary"></i>
                <svg class="icon-md">
                    <use xlink:href="#add"></use>
                </svg>
            </a>
        </div>
    </div>
    {# discount list #}
    <div class="container mt-4">
        <div class="row">
            {% for type_art in type_arts %}
                <div class="col-12">
                    <div class="media mt-2">
                        <div class="media-body">
                            <h5 class="mt-2">{{ type_art.nom }}</h5>
                            {# edit button #}
                            <div class="mt-4">
                                <a href="{{ path('type_art_edit', {'id': type_art.id}) }}" title="Modifier">
                                    <i data-fa-symbol="edit" class="fas fa-pen text-secondary"></i>
                                    <svg class="icon-md">
                                        <use xlink:href="#edit"></use>
                                    </svg>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% else %}
                {# empty #}
                <div class="">
                    Aucune catégorie enregistrée
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
