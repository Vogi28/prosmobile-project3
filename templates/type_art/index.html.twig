{% extends 'base.html.twig' %}

{% block title %}TypeArt index{% endblock %}

{% block body %}
    <h1>TypeArt index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_art in type_arts %}
            <tr>
                <td>{{ type_art.id }}</td>
                <td>{{ type_art.nom }}</td>
                <td>
                    <a href="{{ path('type_art_show', {'id': type_art.id}) }}">show</a>
                    <a href="{{ path('type_art_edit', {'id': type_art.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_art_new') }}">Create new</a>
{% endblock %}
