{% extends 'base.html.twig' %}

{% block title %}PROSmobiles |gestion des catégories{% endblock %}

{% block body %}
    {# tree #}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-white rounded-0">
            <li class="breadcrumb-item">
                <a href="{{ path('admin_index') }}">
                    Dashboard
                </a>
            </li>
            <li class="breadcrumb-item">
                <a href="{{ path('type_art_index') }}">
                    Gestion des catégories
                </a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">
                Configurer la catégorie
            </li>
        </ol>
    </nav>
    {# body #}
    <h1 class="text-center">Modifier la catégorie</h1>
    {# include type_art form #}
    {{ include('type_art/_form.html.twig') }}
    {# delete button #}
    <div class="container mt-2">
        <div class="row">
            <div class="btn" style="width:150px;">
                {{ include('type_art/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
