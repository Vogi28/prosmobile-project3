<form method="post" action="{{ path('type_art_delete', {'id': type_art.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_art.id) }}">
    <button class="btn">Delete</button>
</form>
