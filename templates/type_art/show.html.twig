{% extends 'base.html.twig' %}

{% block title %}TypeArt{% endblock %}

{% block body %}
    <h1>TypeArt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_art.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_art.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_art_index') }}">back to list</a>

    <a href="{{ path('type_art_edit', {'id': type_art.id}) }}">edit</a>

    {{ include('type_art/_delete_form.html.twig') }}
{% endblock %}
