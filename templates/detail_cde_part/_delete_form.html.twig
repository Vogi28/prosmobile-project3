<form method="post" action="{{ path('detail_cde_part_delete', {'id': detail_cde_part.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_cde_part.id) }}">
    <button class="btn">Delete</button>
</form>
