{% extends 'base.html.twig' %}

{% block title %}Composant{% endblock %}

{% block body %}
    <h1>Composant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ composant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ composant.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ composant.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('composant_index') }}">back to list</a>

    <a href="{{ path('composant_edit', {'id': composant.id}) }}">edit</a>

    {{ include('composant/_delete_form.html.twig') }}
{% endblock %}
