{% extends 'base.html.twig' %}

{% block title %}Spec{% endblock %}

{% block body %}
    <h1>Spec</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spec.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ spec.nom }}</td>
            </tr>
            <tr>
                <th>IdGroupe</th>
                <td>{{ spec.idGroupe }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ spec.valeur }}</td>
            </tr>
            <tr>
                <th>Unite</th>
                <td>{{ spec.unite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spec_index') }}">back to list</a>

    <a href="{{ path('spec_edit', {'id': spec.id}) }}">edit</a>

    {{ include('spec/_delete_form.html.twig') }}
{% endblock %}
