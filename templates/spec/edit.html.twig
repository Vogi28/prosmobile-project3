{% extends 'base.html.twig' %}

{% block title %}PROSmobiles |gestion des spécifications{% endblock %}

{% block body %}
    {# tree #}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-white rounded-0">
            <li class="breadcrumb-item">
                <a href="{{ path('admin_index') }}">
                    Dashboard
                </a>
            </li>
            <li class="breadcrumb-item">
                <a href="{{ path('spec_index') }}">
                    Gestion des spécifications
                </a>
            </li>
            <li class="breadcrumb-item active" aria-current="page">
                Configurer la spécification
            </li>
        </ol>
    </nav>
    {# body #}
    <h1 class="text-center">Modifier la spécification</h1>
    {# include type_art form #}
    {{ include('spec/_form.html.twig') }}
    {# delete button #}
    <div class="container mt-2">
        <div class="row">
            <div class="btn" style="width:150px;">
                {{ include('spec/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
